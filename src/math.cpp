#include "my_math.h"
#include <cmath>

int add(int a, int b) {
    return a + b;
}

int multiply(int a, int b) {
    return a * b;
}

double square_root(double x)
{
    return std::sqrt(x);
}
